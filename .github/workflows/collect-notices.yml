name: Collect notices

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch: {}

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger notice collection
        run: curl -sf -X POST https://maple-daily-log-e5su.onrender.com/api/notices/check-new
